from typing import Iterable


def send_due_reminder_emails(_: Iterable[str]) -> int:
    # Scaffold: integrate with actual email provider later
    # Return number of emails that would be sent
    return len(list(_))


